import { create } from 'zustand';

const treeStructrueStore = create( (set) => {
    return {
        treeStructure: null,
        setTreeStructure: () => {
            set({
                
            })
        }
    }
} )